<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdut.mapper.AdminOperationLogMapper">


    <insert id="insert">
        insert into admin_operation_log (admin_id, operation, method, url, params, ip, operation_time)
        values (#{adminId}, #{operation}, #{method}, #{url}, #{params}, #{ip}, #{operationTime})
    </insert>


    <delete id="deleteByIds">
        delete from admin_operation_log where id in
        <foreach item="id" collection="ids" separator="," close=")" open="(" index="">
            #{id}
        </foreach>
    </delete>

    <select id="list" resultType="com.gdut.pojo.AdminOperationLog">
        select * from admin_operation_log
        <where>
            <if test="adminId != null and adminId != '' ">
                and admin_id = #{adminId}
            </if>
            <if test="operation != null and operation != '' ">
                and operation like concat('%',#{operation},'%')
            </if>
            <if test="startTime != null and endTime != null">
                and operation_time between #{startTime} and #{endTime}
            </if>
        </where>
        order by operation_time desc
    </select>


</mapper>
